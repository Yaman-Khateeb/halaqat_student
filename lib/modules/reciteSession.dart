class ReciteSession
{
  final int pagesAmount;
  final double grade;
  final DateTime date;
  String? description;
  ReciteSession({required this.pagesAmount,required this.grade,required this.date, this.description});
}